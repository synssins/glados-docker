"""Per-page HTML modules for the GLaDOS WebUI.

Each sibling module exports an HTML string constant containing its
tab-content div. The shell (_shell.py) wraps them with the common
chrome. Composition happens in glados.webui.tts_ui.
"""
